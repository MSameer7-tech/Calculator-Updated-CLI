# Calculator Project

A simple command-line calculator written in Python. It supports basic arithmetic operations like addition, subtraction, multiplication, and division. 

## Features
- Continuous calculation (use the result of previous calculation for the next one)
- ASCII Art logo
- Clear console and restart calculation

## Usage
Run the script using Python:
```bash
python main.py
```
